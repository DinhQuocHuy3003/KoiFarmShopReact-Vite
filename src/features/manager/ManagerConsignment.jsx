import React, { useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
import useStore from "../../app/store";

export default function ManagerConsignment() {
//   const navigate = useNavigate();
//   const [page, setPage] = React.useState(1);
// const [totalPage, setTotalPage] = React.useState(1);
//   const [listConsignment, setListConsignment] = useState([]);
//   const getAllConsignments = useStore((state) => state.getAllConsignments);
// const acceptConsignment = useStore((state) => state.acceptConsignment);
// const rejectConsignmnet = useStore((state) => state.rejectConsignmnet);

//   useEffect(() => {
//     const getAllConsignmentOnInit = async () => {
//       await getAllConsignments();
//       setConsignment(useStore.getState().requests.data || []);
//     };
//     getAllConsignmentOnInit();
//   }, []);

//   const handleAccept = async (id) => {
//     try {
//         await acceptConsignment(id);
// setListConsignment
//     }
//   }
//   return (
//     <div style={{ padding: "20px" }}>
//     </div>
//   );

  
}
